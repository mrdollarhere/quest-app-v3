# DNTRNG™ Registry Bridge Protocol - Action Registry (v19.2.5)

This document serves as the forensic checklist for all supported handshakes between the Next.js frontend and the Google Apps Script (GAS) backend.

## 1. GET Actions (doGet)
Used for retrieving data from the Google Sheet registry. Parameters are passed via URL search queries.

- [ ] **`getVersion`**: Returns the current script version (v19.2.5).
- [ ] **`getBugReports`**: Retrieves the site-wide issue registry.
- [ ] **`getActivity`**: Retrieves the unified forensic telemetry stream.
- [ ] **`login`**: Verifies credentials.
- [ ] **`getUsers`**: Retrieves the full student registry.
- [ ] **`getTests`**: Fetches the assessment module library.
- [ ] **`getResponses`**: Retrieves mission submission logs.
- [ ] **`getSettings`**: Fetches platform calibration parameters.
- [ ] **`getQuestions`**: Retrieves the full question set for a specific module ID.
- [ ] **`getTestById`**: Fetches metadata for a single assessment module.
- [ ] **`getUserById`**: Fetches metadata for a single student identity.
- [ ] **`getUserStats`**: Aggregates performance history for a specific identity.
- [ ] **`getPublicStats`**: Returns global system volume telemetry.

## 2. POST Actions (doPost)
Used for committing, updating, or purging data in the registry. Payloads are transmitted as JSON bodies.

- [ ] **`saveBugReport`**: Records student-submitted technical discrepancies.
- [ ] **`updateBugStatus`**: Updates triage status and internal notes.
- [ ] **`logActivity` / **`logEvent`**: Records telemetry pulses and IP/Device signatures.
- [ ] **`submitResponse`**: Commits finalized assessment results.
- [ ] **`deleteResponse`**: Purges a specific submission log.
- [ ] **`saveTest`**: Upserts test metadata and initializes question tabs.
- [ ] **`deleteTest`**: Purges a test module and its associated question sheet.
- [ ] **`saveUser`**: Single student provisioning.
- [ ] **`saveUsers`**: Batch identity provisioning.
- [ ] **`deleteUser`**: Purges a specific student identity.
- [ ] **`saveQuestion`**: Single question node calibration.
- [ ] **`saveQuestions`**: Complete registry overwrite for a module's question set.
- [ ] **`saveSetting`**: Calibrates global system preferences.

## 3. Security Protocol
All actions require the `apiKey` parameter/field to match the `INTERNAL_API_KEY` defined in the GAS script.

---
*Generated by DNTRNG™ Forensic Audit Engine*
